{
  "name": "musicbusinessarena",
  "version": "1.0.0",
  "description": "MusicBusiness Arena artist upload, music discovery, streaming links, downloads, donations, and video platform.",
  "main": "server.js",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "build": "node scripts/verify-build.js",
    "check": "node --check server.js && node --check script.js && node --check artist-dashboard.js && node --check artist-page.js && node --check videos.js && node --check store-manager.js"
  },
  "dependencies": {
    "mongodb": "^6.19.0"
  }
}
